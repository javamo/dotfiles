[color]
	status = auto
	log = auto
	ui = auto
[alias]
        remotes = remote -v
        branches = branch -a
        tags = tag -l
        lg = log --color --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
